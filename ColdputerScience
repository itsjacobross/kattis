#include <stdio.h>
#include <math.h>
#include <stdlib.h>

int main()
{
    int n, math, counter=0;
    scanf("%d", &n);
    for(int i=0; i<n; i++){
        scanf("%d", &math);
        if(math>=0){continue;}
        counter++;
    }
    printf("%d",counter);

    return 0;
}

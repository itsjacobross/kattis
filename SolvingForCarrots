#include <stdio.h>
#include <math.h>
#include <stdlib.h>

int main()
{
    int n, p;
    char *description;
    scanf("%d",&n); scanf("%d",&p);
    for(int i=0; i<n; i++){
        scanf("%s",description);
    }
    printf("%d",p);
    return 0;
}

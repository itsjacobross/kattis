#include <stdio.h>
#include <math.h>
#include <stdlib.h>

int main()
{
    int n;
    float q, p, qaly;
    scanf("%d", &n);
    for(int i=0; i<n; i++){
        scanf("%f",&q); scanf("%f",&p);
        qaly = qaly + (q*p);
    }
    printf("%f",qaly);

    return 0;
}
